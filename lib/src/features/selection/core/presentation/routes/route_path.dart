abstract class SelectionRoutePath {
  static const String selection = 'selection';
  static const String searchParams = 'search-params';
  static const String candidate = ':id';
  static const String compatibility = ':id/compatibility';
}

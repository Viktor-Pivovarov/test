import 'package:flutter/rendering.dart';

class EffectsStylesLight {
  static const BoxShadow redGlow = BoxShadow(
    color: Color.fromRGBO(246, 25, 61, 0.3),
    blurRadius: 10,
    offset: Offset(0, 5),
  );
  static const BoxShadow greenGlow = BoxShadow(
    color: Color.fromRGBO(25, 246, 87, 0.3),
    blurRadius: 10,
    offset: Offset(0, 5),
  );
  static const BoxShadow blueGlow = BoxShadow(
    color: Color.fromRGBO(47, 128, 237, 0.3),
    blurRadius: 10,
    offset: Offset(0, 5),
  );
  static const BoxShadow goldenGlow = BoxShadow(
    color: Color.fromRGBO(255, 202, 65, 0.3),
    blurRadius: 10,
    offset: Offset(0, 5),
  );
  static const BoxShadow tabBarShadow = BoxShadow(
    color: Color.fromRGBO(159, 172, 185, 0.3),
    blurRadius: 10,
    offset: Offset(0, 5),
  );
}

class EffectStylesDark {
  static const BoxShadow redGlow = BoxShadow(
    color: Color.fromRGBO(246, 25, 61, 0.3),
    blurRadius: 10,
    offset: Offset(0, 5),
  );
  static const BoxShadow greenGlow = BoxShadow(
    color: Color.fromRGBO(25, 246, 87, 0.3),
    blurRadius: 10,
    offset: Offset(0, 5),
  );
  static const BoxShadow blueGlow = BoxShadow(
    color: Color.fromRGBO(47, 128, 237, 0.3),
    blurRadius: 10,
    offset: Offset(0, 5),
  );
  static const BoxShadow goldenGlow = BoxShadow(
    color: Color.fromRGBO(255, 202, 65, 0.3),
    blurRadius: 10,
    offset: Offset(0, 5),
  );
  static const BoxShadow tabBarShadow = BoxShadow(
    color: Color.fromRGBO(159, 172, 185, 0.3),
    blurRadius: 10,
    offset: Offset(0, 5),
  );
}

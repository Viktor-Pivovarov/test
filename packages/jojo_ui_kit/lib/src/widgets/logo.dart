import 'package:flutter/material.dart';

class UiLogo extends StatelessWidget {
  const UiLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
    //TODO deprecated
    // return CrossPlatformSvg.asset(Assets.media.logo);
  }
}
